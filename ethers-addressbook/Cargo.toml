[package]
name = "ethers-addressbook"
version = "0.1.0"
edition = "2021"

[dependencies]
once_cell = "1.9.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

ethers-core = { path = "../ethers-core" }
